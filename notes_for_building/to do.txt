---------
19 Nov 25

[DONE] 1. Better concurrency for scraping channels - instead of a go routine per channel do a finite worker pattern 
2. Email alerting for errors and error summaries - attach log file 
5. web app to 
	- upload cookies file 
	- input new channels found 
	- show all channels being scraped
	- date last scraped 
	- summary stats 
	- auto add IsRecentlyAdded for new vidoes,
6. do proper rate limiting. Configure ratelimiter in commandmanager and captiondownloader. You need something that is low, but also uses the concurrency stuff? 
7. main.go: 3 ---- HANDLE SHUTDOWN SIGNALS (Ctrl+C, kill, etc.) - this code doesn't work
9. - output.txt in commandmanger, wtf does it do - is this done? 




BETTER SCRAPED AT LOGIC 
 - Even if you have deduping logic via you don't want to scrape all the videos from a channel, can you cut off after a certain upload date? e.g. you've scraped the site yesterday, so if you detecta video from before that date, then stop

EXISTING CHANNELS 
 - UPDATE YT-DLP TO HAVE DATEAFTER VARIABLE TO MINIMIZE HTTP CALLS 
 - UPDATE YT-DLP TO HAVE A NEW FLAG, IF TRUE THEN SCRAPE FROM TODAY BACK ONE YEAR 
 - UPDATE VideoToBeDownloadedResult struct to contain a date representing the youngest video scraped 
 - 

 NEW CHANNEL ADDED 
  - One off scrape to go back one year 
  - update VideoToBeDownloadedResult.oldest_scapted_video_uploaded_at 
  - after run, then remove IsRecentlyAdded from videosources.csv
  - channel enters new channel loop 

Trade off 
- you already have the hashtable with existing youtube videos, so there is potential to have double deduping whereby
you throttle volume by being smarter with last_uploaded_at; but you still get back a list of channels but only reqest
suhbtitles via an additional network call if it doesn't exist in the hashmap